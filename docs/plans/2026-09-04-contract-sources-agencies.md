# UK contract/interim technology-leadership recruiters: ATS-board sweep

Research pass only — no config or source-list files were edited. Every board
below was checked with `python3 -m jobradar.cli discover` (no `--add`) and
then verified with one direct fetch through `jobradar.fetch.fetch_one` +
`jobradar.adapters.parse`, exactly the code path a real scan uses.

DRAFT IN PROGRESS — being filled in as the discover/verify sweep completes.
